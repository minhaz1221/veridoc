{
  "name": "@veridoc/cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "veridoc": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint src"
  },
  "dependencies": {
    "@veridoc/core": "workspace:*",
    "commander": "^12.1.0",
    "dotenv": "^16.4.0",
    "ethers": "^6.13.0",
    "pdf-lib": "^1.17.1",
    "qrcode": "^1.5.3"
  },
  "devDependencies": {
    "@types/qrcode": "^1.5.5",
    "@vitest/coverage-v8": "^2.0.0",
    "vitest": "^2.0.0"
  }
}
